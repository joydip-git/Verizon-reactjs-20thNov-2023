// const { add: add, subtract: subtract } = require('./utility')
// const { add, subtract } = require('./utility')

import { add, subtract } from "./utility";

const addRes = add(12, 3)
console.log(addRes)

const subRes = subtract(12, 3)
console.log(subRes)